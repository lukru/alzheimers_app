<% if @comment.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

    <ul>
    <% @comment.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <div class='form-group'>
  <%= f.label :author_name, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :author_name, class: "form-control", placeholder: "Enter name" %>
    </div>
  </div>
<br><br><br>
  <div class='form-group'>
  <%= f.label :comment_text, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_area :comment_text, class: "form-control", placeholder: "Enter comment here" %>
    </div>
  </div>
<br><br><br>
  <div class="field">
    <%= f.hidden_field :user_id %><br>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Create comment", class: "btn btn-default" %>
    </div>
  </div>
<h1>Listing tips</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Activity type</th>
      <th>Description</th>
      <th>References</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tips.each do |tip| %>
      <tr>
        <td><%= tip.name %></td>
        <td><%= tip.activity_type %></td>
        <td><%= tip.description %></td>
        <td><%= tip.references %></td>
        <td><%= link_to 'Show', tip %></td>
        <td><%= link_to 'Edit', edit_tip_path(tip) %></td>
        <td><%= link_to 'Destroy', tip, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tip', new_tip_path %>
